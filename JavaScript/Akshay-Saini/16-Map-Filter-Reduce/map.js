const arr = [1, 2, 3, 4, 5];

const double = arr.map(ele => ele * 2);
console.log(double);

const triple = arr.map(ele => ele * 3);
console.log(triple);

const binary = arr.map(ele => ele.toString(2));
console.log(binary);